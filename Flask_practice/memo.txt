仮想環境venv フォルダの作成
$ python3 -m venv venv 

anacondaの仮想環境から抜け出す
$ conda deactivate

venvフォルダの仮想環境に適応する
$ . venv/bin/activate 

どのファイルを有効化するか
$ export FLASK_APP=hello.py

git キャッシュ削除
$ git rm -r --cached .  

